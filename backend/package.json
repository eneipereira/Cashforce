{
  "name": "backend-cashforce",
  "version": "1.0.0",
  "description": "Backend do teste técnico",
  "main": "src/server.js",
  "scripts": {
    "db": "npx sequelize-cli db:drop && npx sequelize-cli db:create && npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "start": "node -r dotenv/config .",
    "dev": "nodemon -r dotenv/config .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Enei Pereira Junior",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^2.3.3",
    "sequelize": "^6.28.0"
  },
  "devDependencies": {
    "eslint": "^8.31.0",
    "eslint-config-trybe-backend": "^2.1.0",
    "eslint-plugin-import": "^2.26.0",
    "nodemon": "^2.0.20",
    "sequelize-cli": "^6.5.2"
  }
}
